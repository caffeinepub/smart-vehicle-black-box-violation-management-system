# SafeDrive Traffic Violation Monitoring System

## Current State
- Camera section in DashboardPage uses open-in-new-tab only (no embedded stream, no auto-load)
- No camera status indicators (Online/Offline, Recording Active/Stopped)
- Demo vehicle number is KL07AB1234
- Evidence section in challan shows first violation image, not the last
- Challan footer says SAFEWAY branding; user wants SafeDrive branding and footer update
- Challan header has no MVD Logo left / SafeDrive logo right split layout
- Multiple violation popup message exists but needs exact wording update
- No red indicator for repeated violations in the table

## Requested Changes (Diff)

### Add
- Auto-loading embedded MJPEG streams in the camera monitoring section (on page open)
- "Camera temporarily unavailable" fallback message on stream error
- Camera Status: Online/Offline indicator card on dashboard
- Recording Status: Active/Stopped indicator card on dashboard
- Red indicator/badge for repeated violations (same type appearing more than once)
- MVD Logo on left side of challan header, SafeDrive Project Logo on right side

### Modify
- Camera cards: switch from open-in-new-tab-only to auto-loading embedded `<img>` MJPEG stream with error fallback; keep the "View in New Tab" button as supplement
- Vehicle numbers: change all hardcoded demo vehicle numbers from "KL07AB1234" to "KL59AB 1234"
- Challan evidence section: use the LAST violation image (highest index with imageUrl), not the first
- Challan footer text: "Generated by SafeDrive Intelligent Traffic Monitoring System"
- PDF challan footer: same footer text update
- Multiple violation popup message to exactly: "⚠ Multiple Violations Detected. Challan Generated."
- Violation table: highlight rows with score >= 5 in red, add red dot indicator for repeated violations

### Remove
- Nothing removed

## Implementation Plan
1. DashboardPage.tsx: Rework `CameraCard` to auto-load `<img src={streamSrc}>` on mount, add onerror fallback "Camera temporarily unavailable", add online/offline status dot, add Camera Status and Recording Status indicator cards in the system status area
2. DashboardPage.tsx: Replace KL07AB1234 with KL59AB 1234 wherever hardcoded
3. ChallanPreviewPage.tsx: Update evidence to use last violation image; update footer to "Generated by SafeDrive Intelligent Traffic Monitoring System"; update challan header to show MVD logo left + SafeDrive logo right layout
4. ChallanPreviewModal.tsx: Same evidence + footer updates
5. CenterAlertPopup.tsx: Update multipleViolation title/lines to match required message
6. LiveViolationsPage.tsx: Add red badge for repeated violation types, highlight score>=5 rows, replace KL07AB1234 with KL59AB 1234
